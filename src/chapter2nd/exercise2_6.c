//
// Created by RichardL on 2025/10/15.
//
#include <stdio.h>

int main(void) {
    const int toes = 10;
    const int twice = toes * 2;
    const int square = toes * toes;
    printf("toes: %d, twice: %d, square: %d\n", toes, twice, square);
}